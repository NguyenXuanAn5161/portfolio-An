import One from "../slides/One";
import Two from "../slides/Two";
import "./Home.scss";

const Home = (props) => {
  return (
    <div className="wrapper">
      <One />
      <Two />
    </div>
  );
};

export default Home;
